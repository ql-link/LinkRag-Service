<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingluo.link.service.mapper.SysUserMapper">

    <select id="selectByUsername" resultType="com.qingluo.link.model.entity.SysUser">
        SELECT * FROM sys_user WHERE username = #{username}
    </select>

    <update id="updateLastLoginTime">
        UPDATE sys_user SET last_login_at = #{lastLoginAt} WHERE id = #{userId}
    </update>

    <insert id="insert" parameterType="com.qingluo.link.model.entity.SysUser">
        INSERT INTO sys_user (id, username, password_hash, nickname, email, role, status, created_at, updated_at)
        VALUES (#{id}, #{username}, #{passwordHash}, #{nickname}, #{email}, #{role}, #{status}, #{createdAt}, #{updatedAt})
    </insert>

</mapper>