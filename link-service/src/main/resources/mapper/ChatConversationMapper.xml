<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qingluo.link.service.mapper.ChatConversationMapper">

    <select id="selectByUserId" resultType="com.qingluo.link.model.entity.ChatConversation">
        SELECT * FROM chat_conversation
        WHERE user_id = #{userId} AND is_deleted = 0
        ORDER BY is_pinned DESC, updated_at DESC
    </select>

    <update id="updateById">
        UPDATE chat_conversation
        SET title = #{title}, last_config_id = #{lastConfigId}, last_model_name = #{lastModelName},
            is_pinned = #{isPinned}, updated_at = #{updatedAt}
        WHERE id = #{id}
    </update>

    <update id="deleteById">
        UPDATE chat_conversation SET is_deleted = 1 WHERE id = #{id}
    </update>

</mapper>